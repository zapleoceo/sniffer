"""Справочник Chotot: всё, что снято с API опытным путём.

Вынесено из адаптера отдельно, потому что это не код, а наблюдения. API
недокументирован (spec-v2, 6.2), у Chotot нет ни списка регионов, ни списка
категорий: `static.chotot.com/storage/chotot-parameters/regions_v2.json` и
`gateway.chotot.com/v1/public/chotot-regions` отдают 404. Значения ниже сняты
живыми запросами 2026-08-29 и протухнут молча — искать их надо здесь, а не по
всему адаптеру.

Как перепроверить: запрос без фильтра по региону, затем пары
(`region_v2`, `region_name`) из поля каждого объявления.
"""

from __future__ import annotations

from sniffer.domain.passport import Category

SOURCE_NAME = "chotot"

GATEWAY_URL = "https://gateway.chotot.com/v1/public/ad-listing"
# Короткая форма ссылки: отвечает 200 и редиректит на канонический адрес вида
# xe.chotot.com/mua-ban-xe-may-thanh-pho-nha-trang-khanh-hoa/<list_id>.htm.
# Собирать канонический самим — значит завязаться на слаги категорий.
LISTING_URL = "https://www.chotot.com/{list_id}.htm"

# Gateway отвечает и без заголовка, но недокументированный публичный эндпоинт
# не то место, где стоит выделяться из обычного браузерного трафика.
USER_AGENT = (
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
    "(KHTML, like Gecko) Chrome/126.0.0.0 Safari/537.36"
)

# Сервер режет limit до 50 молча: запрос limit=1000 вернул 50 объявлений.
MAX_LIMIT = 50
DEFAULT_LIMIT = 20

# Тип объявления, который шлёт сам сайт. На выдачу для cg=2020 не влияет
# (total одинаковый с ним и без него), но повторять запрос живого клиента у
# недокументированного API безопаснее, чем изобретать свой.
AD_TYPE = "s,k"

# Обратная проверка кода Кханьхоа: с region_v2=7044 все 50 объявлений первой
# страницы вернулись с region_name «Khánh Hòa». Нумерация не сплошная —
# соседние круглые числа пустые, угадать код нельзя, только снять.
REGION_V2: dict[str, int] = {
    "nha_trang": 7044,  # Khánh Hòa
    "da_nang": 3017,
    "ha_noi": 12000,
    "ho_chi_minh": 13000,
}

# Кханьхоа тянется на сотню километров: Cam Ranh (704402) и Ninh Hòa (704405)
# лежат в той же провинции, но байк оттуда клиенту в Нячанге бесполезен.
# Поэтому по умолчанию сужаем до района города.
AREA_V2: dict[str, int] = {
    "nha_trang": 704401,  # Thành phố Nha Trang
}

# cg — категория Chotot. 2020 «Xe máy» проверен живым запросом.
CATEGORY_CG: dict[Category, int] = {
    Category.MOTORBIKE: 2020,
}

# ---------------------------------------------------------------------------
# Структурные поля мотобайков. Снято живыми запросами 2026-08-31.
#
# Это главный инструмент точности, а не `q`. Замер по Нячангу (cg=2020, всего
# 59 объявлений): общий запрос по категории даёт 42 настоящих скутера-автомата
# из 59 (71%) — в выдачу лезут Honda Winner X, Yamaha R15, KTM Duke 390 и
# 50-кубовые мопеды с лапкой. `motorbiketype=1` даёт 41 из 41 (100%), потеряв
# ровно одно объявление из 42, которое сам продавец записал в «côn tay».
#
# Текстовый `q` для этого не годится: это не текстовый поиск, а смесь поиска с
# фасетным словарём, и ведёт себя непредсказуемо. «tay ga» вернуло те же самые
# 41 объявление, что `motorbiketype=1` (пересечение 41, разницы ноль), «xe ga»
# — ноль, «xe máy» и «nguyên zin» — все 59, то есть не отфильтровали ничего.
# Полагаться на такое нельзя, а на поле — можно.
#
# Справочника значений у Chotot нет: `/v1/public/ad-listing/parameters` отдаёт
# 400, `static.chotot.com/storage/chotot-parameters/*` — 404. Значения ниже
# восстановлены из выдачи и проверены обратным запросом.

# motorbiketype — тип кузова, он же способ переключения передач. Заполнен у
# ВСЕХ объявлений и разбивает выдачу без остатка: 41 + 5 + 12 + 1 = 59 из 59.
# Поэтому годится как жёсткий фильтр — в отличие от motorbikecapacity ниже.
MOTORBIKE_TYPE_AUTOMATIC = 1  # xe tay ga — вариатор, «скутер» в русском смысле
MOTORBIKE_TYPE_FOOTSHIFT = 2  # xe số — лапка без ручного сцепления (Wave, Sirius)
MOTORBIKE_TYPE_MANUAL = 3  # côn tay — ручное сцепление (Winner X, Exciter, R15)
MOTORBIKE_TYPE_ELECTRIC = 4  # xe điện — электро (VinFast, Yadea, Pega)

# transmission паспорта → motorbiketype. «semi» это именно xe số: в переписи
# нашлось «Daelim Sirius 50cc 2019 Bán tự động» с motorbiketype=2.
TRANSMISSION_TYPE: dict[str, int] = {
    "automatic": MOTORBIKE_TYPE_AUTOMATIC,
    "semi": MOTORBIKE_TYPE_FOOTSHIFT,
    "manual": MOTORBIKE_TYPE_MANUAL,
}

# motorbikecapacity — корзины объёма двигателя. Границы восстановлены по «NNcc»
# в тексте 100 объявлений на корзину: 1 — все ровно 50; 2 — 50…100; 3 — 100…175
# (p10=100, p90=160); 4 — 175 и выше (200, 300, 400, 750, 1140).
#
# ВАЖНО: поле заполнено НЕ везде — 35 объявлений из 59 по Нячангу. Жёсткий
# фильтр по нему выбрасывает не только неподходящие байки, но и все, где
# продавец объём не указал. Поэтому применяется только когда клиент спросил
# про объём сам.
CAPACITY_BUCKETS: tuple[tuple[int, int], ...] = (
    # (верхняя граница cc включительно, код корзины)
    (50, 1),
    (100, 2),
    (175, 3),
)
CAPACITY_ABOVE_ALL = 4

# Корзина 5 существует (320 объявлений по стране), но расшифровке не поддалась:
# в ней Honda Vision, AirBlade 125, SH125, Vario 150 и электро VinFast вместе, а
# «NNcc» в тексте нет ни у одного из 100 просмотренных. Не используется.

# motorbikebrand — бренд. Проверено обратным запросом: по каждому коду
# 20–27 из 30 заголовков содержат именно это имя. Vespa — марка Piaggio и
# лежит в том же коде 3.
MOTORBIKE_BRAND: dict[str, int] = {
    "honda": 1,
    "yamaha": 2,
    "piaggio": 3,
    "vespa": 3,
    "suzuki": 4,
    "sym": 5,
    "benelli": 8,
    "bmw": 9,
    "daelim": 11,
    "ktm": 20,
    "kymco": 21,
    "gpx": 38,
    "pega": 74,
    "victoria": 80,
}

# condition_ad — состояние, объявленное продавцом. Снято живым запросом
# 31.08.2026: `condition_ad=1` («Đã sử dụng», б/у) — 58, `=2` («Mới», новое) —
# 1, `=3` — ноль. 58 + 1 = 59 без пересечений, и каждое объявление несёт своё
# значение в ответе, то есть поле настоящее.
#
# Поле есть — едет поле (spec-v2 2.2, правило 7). До этого `condition=new`
# уходило словом `q='xe mới'`, и фильтром оно не было: те же 8 объявлений, что
# по `q='xe'`, все восемь помечены б/у, единственное новое не найдено.
#
# Отображается ТОЛЬКО `new`. «good» и «worn» — градации внутри б/у, которых у
# поля нет; отправить «good» в `condition_ad=1` значит выбросить объявление,
# помеченное новым, хотя новый байк «отличному состоянию» удовлетворяет.
#
# Честно про масштаб: весь сегмент «Mới» здесь — ОДНО объявление, и это Honda 67
# (конструкция 1960-х), которую продавец пометил новой. Поле передаёт слова
# продавца, а не факт, поэтому настоящее предложение новых байков живёт не на
# этой доске, а у дилеров и в чатах. Ноль по «новый + 125cc» — честный ноль
# поля, а не пустота от лишнего слова.
CONDITION_AD_USED = 1
CONDITION_AD_NEW = 2
CONDITION_PARAM = "condition_ad"
CONDITION_AD: dict[str, int] = {"new": CONDITION_AD_NEW}

# regdate — год регистрации, семантика «не раньше этого года». Замер по Нячангу:
# 2010 → 50, 2015 → 37, 2020 → 17, 2025 → 2, 2026 → 1 (в переписи ровно одно
# объявление 2026 года). Монотонно убывает, значит это нижняя граница.
REGDATE_PARAM = "regdate"

# price — диапазон «min-max» в донгах. Замер: 0-5 млн → 10, 0-20 млн → 38,
# 0-50 млн → 55, 20 млн-1 млрд → 21; 38 + 21 = 59, то есть границы не врут.
PRICE_PARAM = "price"
PRICE_CURRENCY = "VND"
PRICE_MAX_VND = 10_000_000_000

# Фильтры, которые план вправе назвать прямым именем Chotot. Список закрытый:
# гейт против выдумки модели, которая иначе отправит в query-string что угодно.
# Все проверены замером по Нячангу (59 объявлений без фильтра): motorbiketype
# 41/5/12/1, motorbikecapacity 7/0/27/1, motorbikebrand=1 → 26, regdate=2020 →
# 17, price=0-20 млн → 38, condition_ad 58/1/0. Многозначная форма работает
# через запятую:
# motorbiketype=1,3 → 53, а «1|3» и «1-3» отвечают 400.
PLAN_FILTERS: tuple[str, ...] = (
    "motorbiketype",
    "motorbikecapacity",
    "motorbikebrand",
    REGDATE_PARAM,
    PRICE_PARAM,
    CONDITION_PARAM,
)
