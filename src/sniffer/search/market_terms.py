"""Слова рынка — таблицы, а не логика.

Отделено от `vocabulary.py` намеренно: там функции доступа и профили источников,
здесь только данные. Добавить город, категорию или синоним — правка этого файла
и ничего больше; ни одного `if` по названию города или источника нигде нет.

Откуда взяты значения — помечено у каждой таблицы. Три источника доказательств,
и они не равны по силе:

- **замер** — живой запрос к Chotot 31.08.2026, число в комментарии;
- **текст объявлений** — слово встречено в `subject`/`body` реальной выдачи;
- **полевые заметки** — ручной поиск байка в Нячанге 14–17.08.2026 (spec-v2, 7),
  проверено человеком в Telegram, нашим кодом не измерено.

Непроверенное слово добавлять можно, но с пометкой: словарь, в котором нельзя
отличить замер от догадки, через полгода станет догадкой целиком.

И отдельно: **«слово для чата» и «слово, безопасное как `q` у структурной
доски» — разные вещи**, и по самому слову этого не видно. Доска складывает `q`
со своими структурными фильтрами через И, поэтому слово о свойстве гасит верный
фильтр в ноль: `motorbiketype=3` отдаёт 12 объявлений, а `motorbiketype=3` плюс
`q='côn tay'` — ноль. Разница выражена данными: `ATTRIBUTE_TERMS` — слова для
прозы, `BOARD_ATTRIBUTE_TERMS` — измеренное подмножество, безопасное как `q`.
"""

from __future__ import annotations

from sniffer.domain.passport import Category, Intent

# Языки рынка Нячанга. Порядок = порядок убывания плотности объявлений.
MARKET_LANGS: tuple[str, ...] = ("ru", "vi", "en")

LangTerms = dict[str, tuple[str, ...]]

# Существительные и разговорные названия предмета. Глаголы сделки живут отдельно,
# в INTENT_TERMS: одно и то же «tay ga» ищется и в продаже, и в аренде.
#
# Вьетнамские слова для мотобайка выверены замером (Нячанг, cg=2020, всего 59):
#   «tay ga» — 41, «xe tay ga» — 2, «xe số» — 5, «xe máy» — 59 (то же, что без
#   запроса, то есть фильтром не работает), «xe ga» — 0.
# «xe ga» стояло здесь первым и не находило НИЧЕГО; заменено на «tay ga».
CATEGORY_TERMS: dict[Category, LangTerms] = {
    Category.MOTORBIKE: {
        "ru": ("скутер", "байк", "мопед", "мотобайк"),
        "vi": ("tay ga", "xe tay ga", "xe số"),
        "en": ("scooter", "used motorbike", "moped"),
    },
    Category.BICYCLE: {
        "ru": ("велосипед", "велик"),
        "vi": ("xe đạp",),
        "en": ("bicycle",),
    },
    Category.CAR: {
        "ru": ("машина", "авто"),
        "vi": ("ô tô cũ", "xe hơi"),
        "en": ("used car",),
    },
    Category.APARTMENT: {
        "ru": ("квартира", "апартаменты", "студия"),
        "vi": ("căn hộ", "chung cư"),
        "en": ("apartment", "studio"),
    },
    Category.ROOM: {
        "ru": ("комната",),
        "vi": ("phòng trọ", "phòng cho thuê"),
        "en": ("room",),
    },
    Category.HOUSE: {
        "ru": ("дом", "вилла"),
        "vi": ("nhà nguyên căn", "biệt thự"),
        "en": ("house", "villa"),
    },
}

# Намерение клиента переворачивается в намерение автора объявления: клиент
# покупает — ищем тех, кто продаёт.
INTENT_TERMS: dict[Intent, LangTerms] = {
    Intent.BUY: {"ru": ("продам",), "vi": ("bán",), "en": ("for sale",)},
    Intent.RENT: {"ru": ("сдам",), "vi": ("cho thuê",), "en": ("for rent",)},
    Intent.SELL: {"ru": ("куплю",), "vi": ("cần mua",), "en": ("wanted",)},
    Intent.RENT_OUT: {"ru": ("сниму",), "vi": ("cần thuê",), "en": ("looking to rent",)},
}

# Документы на байк — одно знание с двумя потребителями: это и значение
# атрибута `papers=blue_card`, и то, чем продавец хвастается независимо от
# паспорта. Поэтому слова лежат здесь один раз, а обе таблицы ниже на них
# ссылаются: разъехаться двум копиям больше негде.
#
# «блюкарт» — полевые заметки: находил лоты, которых не находил «скутер».
# «cà vẹt» — вьетнамское название того же документа, встречено в тексте
# объявлений («cavet đầy đủ»). Как запрос к доске все три дают ноль — см.
# BOARD_QUERY_HITS, поэтому в BOARD_ATTRIBUTE_TERMS их нет.
PAPERS_WORDS: LangTerms = {
    "ru": ("блюкарт", "синяя карта", "документы есть"),
    "vi": ("cà vẹt", "cavet", "giấy tờ đầy đủ"),
    "en": ("blue card", "papers"),
}

# Как звучит значение атрибута на языке рынка. Нужно там, где структурного
# фильтра нет: в телеграм-чате «автомат» приходится искать словом, потому что
# фасета «коробка передач» у чата не существует.
#
# ЭТО СЛОВА ДЛЯ ПРОЗЫ. В `q` структурной доски они не идут: там свойство —
# отдельное поле, а слово о свойстве гасит это поле в ноль (замер в
# BOARD_QUERY_HITS). Безопасное подмножество — BOARD_ATTRIBUTE_TERMS.
#
# Вьетнамские слова про коробку — замер по Нячангу: «tay ga» 41, «xe số» 5,
# «côn tay» 0 локально и 264 по стране. «bán tự động» дало 0 запросом, но
# встречается в тексте объявлений («Daelim Sirius 50cc 2019 Bán tự động»).
ATTRIBUTE_TERMS: dict[Category, dict[str, dict[str, LangTerms]]] = {
    Category.MOTORBIKE: {
        "transmission": {
            "automatic": {
                "ru": ("автомат", "вариатор"),
                "vi": ("tay ga", "xe tay ga"),
                "en": ("automatic", "twist and go"),
            },
            "manual": {
                "ru": ("механика", "на передачах"),
                "vi": ("côn tay", "xe côn tay"),
                "en": ("manual", "clutch"),
            },
            "semi": {
                "ru": ("полуавтомат",),
                "vi": ("xe số", "bán tự động"),
                "en": ("semi-automatic",),
            },
        },
        # Слова документов лежат в PAPERS_WORDS: то же знание нужно и жаргону.
        "papers": {"blue_card": PAPERS_WORDS},
        "condition": {
            "new": {"ru": ("новый",), "vi": ("xe mới",), "en": ("new",)},
            # «nguyên zin» / «chính chủ» как запрос к Chotot дают всю выдачу
            # целиком (59 из 59) — фильтром не работают, но в тексте чата это
            # ровно то, чем продавец хвастается.
            "good": {
                "ru": ("отличное состояние", "зверь"),
                "vi": ("nguyên zin", "chính chủ"),
                "en": ("excellent condition",),
            },
            "worn": {"ru": ("требует вложений",), "vi": ("xe cũ",), "en": ("needs work",)},
        },
    },
    Category.APARTMENT: {
        "furnished": {
            "true": {
                "ru": ("с мебелью", "меблированная"),
                "vi": ("full nội thất", "đầy đủ nội thất"),
                "en": ("furnished",),
            },
            "false": {"ru": ("без мебели",), "vi": ("nhà trống",), "en": ("unfurnished",)},
        },
        "sea_view": {
            "true": {
                "ru": ("вид на море",),
                "vi": ("view biển",),
                "en": ("sea view", "ocean view"),
            },
        },
    },
}

# Полная выдача структурной доски на момент замера — знаменатель для
# BOARD_QUERY_HITS: Chotot, Нячанг (region_v2=7044, area_v2=704401, cg=2020).
BOARD_QUERY_TOTAL = 59

# Замер слова КАК ЗАПРОСА `q` к структурной доске, 31.08.2026. Значение — сколько
# объявлений слово отдаёт из BOARD_QUERY_TOTAL; в паре с фильтром указано, что
# получилось вместе с ним.
#
# Таблица существует потому, что по слову не видно, можно ли его отправить доске,
# а последствия несимметричны: лишнее слово в `q` не «немного сужает выдачу», а
# гасит верный структурный фильтр в НОЛЬ, и пустая выдача читается клиентом как
# «на рынке нет». Числа ниже — единственное основание пускать слово в `q`.
#
# Ноль и полная выдача одинаково дисквалифицируют (spec-v2 2.2, правила 4 и 5):
# первое лжёт клиенту, второе не фильтрует ничего и тратит задачу из бюджета.
BOARD_QUERY_HITS: dict[str, int] = {
    # Предмет. Ни одно слово не годится: «tay ga» и «xe số» — это не названия
    # предмета, а те же самые motorbiketype=1 и =2 словами (пересечение полное,
    # разницы ноль), «xe máy» не фильтрует ничего, «xe ga» не находит НИЧЕГО.
    "tay ga": 41,
    "xe tay ga": 2,
    "xe số": 5,
    "xe máy": 59,
    "xe ga": 0,
    "xe": 8,
    # Коробка передач. Доска отбирает её полем motorbiketype (41/5/12/1 из 59),
    # и слово об этом же свойстве только гасит поле: с motorbiketype=3 запросы
    # «côn tay», «xe côn tay» и «tay ga» дают ноль при 12 без запроса.
    "côn tay": 0,
    "xe côn tay": 0,
    "bán tự động": 0,
    # Документы. Ни одного попадания ни в одном написании, при 59 без запроса.
    "cà vẹt": 0,
    "cavet": 0,
    "giấy tờ đầy đủ": 0,
    # Состояние. «chính chủ» и «nguyên zin» отдают все 59 — spec-v2 2.2 правило
    # 5 прямо называет такое слово не фильтром. «xe cũ» не находит ничего.
    #
    # «xe mới» — 8 из 59, и ЭТО ЧУЖОЕ ЧИСЛО. Проверка 31.08.2026: множество его
    # находок совпадает с `q='xe'` до последнего id (8 из 8), а «mới» отдельно
    # отдаёт все 59 — то есть слова «новый» в запросе как будто нет вовсе. Все
    # восемь помечены `condition_ad=1` («Đã sử dụng», б/у), а единственное
    # объявление доски с `condition_ad=2` в эти восемь не входит: слово находит
    # РОВНО ОБРАТНОЕ тому, что означает.
    #
    # Само число 8 этого не показывало — оно не ноль и не вся выдача, то есть
    # прежнего критерия «измерено и в границах» слову хватило. Поэтому критерий
    # закрыт по-другому: `is_board_safe()` сверяет число фразы с числом её
    # измеренной части, и фраза, повторяющая чужое число, безопасной не считается.
    # Для этого «mới» и «xe» лежат здесь замерами, а не в комментарии.
    "chính chủ": 59,
    "nguyên zin": 59,
    "xe cũ": 0,
    "xe mới": 8,
    "mới": 59,
    # Глагол сделки приставкой к слову — приём прозы, доске он ломает даже
    # рабочее слово: «bán nguyên zin», «bán cavet», «bán xe mới» — ноль каждое.
    "bán nguyên zin": 0,
    "bán cavet": 0,
    "bán xe mới": 0,
    # Бренд. Слово и поле дают одно и то же, поэтому едет поле: оно не зависит
    # от написания, которое прислал клиент.
    "honda": 26,  # ровно столько же, сколько motorbikebrand=1
}

# Слова атрибутов, безопасные как `q` у структурной доски. WHITELIST, а не
# blacklist: доска складывает `q` с фильтрами через И, и цена ошибки — пустая
# выдача, которую клиент прочтёт как «на рынке нет». Слово попадает сюда только
# с числом в BOARD_QUERY_HITS, и только если это число не ноль, не вся выдача и
# не повторяет число своей же измеренной части (`is_board_safe()`).
#
# ПУСТО, и это результат замера, а не пробел. Из всего ATTRIBUTE_TERMS сюда
# добралось одно слово — «xe mới», — и оно отсюда убрано: его 8 из 59 оказались
# числом слова «xe», все восемь находок помечены б/у, а настоящее «новое» они не
# находят (см. BOARD_QUERY_HITS). Остальные не прошли раньше: коробку доска
# отбирает полем motorbiketype, и слово о ней гасит поле в ноль; документы не
# находят ничего ни в одном написании; «отличное состояние» находит всё.
#
# То есть слов, которые доске можно отправить о свойстве, у нас НЕТ НИ ОДНОГО —
# свойства отбираются полями, включая `condition` (поле `condition_ad`
# существует: 58 б/у и 1 новое из 59). Пустая таблица здесь честнее непустой:
# лучше отдать 12 объявлений одним фильтром, чем ноль с красивым словом.
BOARD_ATTRIBUTE_TERMS: dict[Category, dict[str, dict[str, LangTerms]]] = {}

# Строки, которые вправе уехать в `q` источнику, ищущему полями. Отдельно от
# BOARD_ATTRIBUTE_TERMS, потому что там слова СВОЙСТВ из паспорта, а здесь —
# гейт на ЛЮБОЙ `q`, включая присланный моделью: пускать её текст к доске без
# замера значит отдать клиенту пустоту вместо выдачи (spec-v2 4.1.1).
#
# Пусто по той же причине, и пустота не случайна: у слов, прошедших замер
# самостоятельным числом («tay ga» 41, «xe số» 5, «honda» 26), доска и так
# имеет своё поле, а слово о том же свойстве с ЧУЖИМ значением поля даёт ноль.
# Появится слово, которое сужает выдачу и не дублирует поле — это одна строка
# здесь плюс его число в BOARD_QUERY_HITS.
BOARD_SAFE_QUERIES: tuple[str, ...] = ()

# Жаргон рынка: то, чем продавец хвастается и что покупатель проверяет. Предмет
# им не назван, поэтому это отдельный слой поверх CATEGORY_TERMS, а не замена.
#
# Идёт ТОЛЬКО в источники со свободным текстом (см. SOURCE_PROFILES): замер по
# Chotot — «скутер» 0, «инжектор» 0, «блюкарт» 0. Структурная доска жаргона не
# знает, и запрос жаргоном там просто теряет запрос из бюджета плана.
JARGON: dict[Category, LangTerms] = {
    Category.MOTORBIKE: {
        # «инжектор» — полевые заметки (spec-v2, 7): находил лоты, которых не
        # находило слово «скутер». То же про документы, и они берутся из
        # PAPERS_WORDS: жаргон и атрибут `papers` — одно знание, и лежать оно
        # должно в одном месте. Остальное добавлено по той же логике «слово
        # продавца, а не покупателя» и живым поиском НЕ проверено.
        "ru": (
            "инжектор",
            *PAPERS_WORDS["ru"],
            "на ходу",
            "без вложений",
            "переоформление",
            "свежая резина",
        ),
        # «xe zin» — замер: 8 из 59 по Нячангу, то есть фильтрует. «máy êm» — 1.
        "vi": ("xe zin", "máy êm", *PAPERS_WORDS["vi"]),
        "en": ("fuel injection", *PAPERS_WORDS["en"], "ready to ride"),
    },
    Category.APARTMENT: {
        "ru": ("длительный срок", "без депозита", "хозяин"),
        "vi": ("chủ nhà", "giá tốt"),
        "en": ("long term", "direct owner"),
    },
}

# Город в паспорте нормализован (`nha_trang`), а искать надо тем написанием,
# которым его пишут в объявлениях. Одно написание на язык: `intake_rules` строит
# из этой таблицы regex распознавания города, и второе написание там означало бы
# вторую альтернативу в шаблоне, а не второй запрос.
CITY_NAMES: dict[str, dict[str, str]] = {
    "nha_trang": {"ru": "Нячанг", "vi": "Nha Trang", "en": "Nha Trang"},
    "da_nang": {"ru": "Дананг", "vi": "Đà Nẵng", "en": "Da Nang"},
}

# Города, которые мы узнаём, но пока не обслуживаем. Узнавать их обязательно, и
# причина не в вежливости: неузнанный город подставляется городом по умолчанию,
# и «ищу скутер в Хойане» после «ищу скутер в Нячанге» приходит с тем же
# намерением, категорией и городом — то есть неотличимо от повтора прежней
# просьбы. Клиент получал переспрос вместо ответа.
#
# Список короткий намеренно: сюда попадают только направления, куда переезжают с
# нашего рынка. Каждое лишнее название — риск поймать город там, где его нет
# («hue» — обычное английское слово), а ложный город означает отказ искать.
OTHER_CITY_NAMES: dict[str, dict[str, str]] = {
    "hoi_an": {"ru": "Хойан", "vi": "Hội An", "en": "Hoi An"},
    "da_lat": {"ru": "Далат", "vi": "Đà Lạt", "en": "Da Lat"},
    "vung_tau": {"ru": "Вунгтау", "vi": "Vũng Tàu", "en": "Vung Tau"},
    "phan_thiet": {"ru": "Фантьет", "vi": "Phan Thiết", "en": "Phan Thiet"},
    "phu_quoc": {"ru": "Фукуок", "vi": "Phú Quốc", "en": "Phu Quoc"},
    "ha_noi": {"ru": "Ханой", "vi": "Hà Nội", "en": "Hanoi"},
    "ho_chi_minh": {"ru": "Хошимин", "vi": "Hồ Chí Minh", "en": "Ho Chi Minh"},
}

ALL_CITY_NAMES: dict[str, dict[str, str]] = {**CITY_NAMES, **OTHER_CITY_NAMES}

# Разговорные написания, которых нет ни в справочнике, ни в слаге. Отдельно от
# `ALL_CITY_NAMES`, потому что это не название на языке, а просто ещё одно слово
# для поиска: «Сайгон» и «Хошимин» — один город.
CITY_ALIASES: dict[str, tuple[str, ...]] = {
    "ho_chi_minh": ("Сайгон", "Saigon", "Sài Gòn", "HCMC"),
    "nha_trang": ("Ня Чанг",),
}
