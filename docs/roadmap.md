# План работ

Что сделано, что осталось, в каком порядке и по какому процессу. Ведётся как
рабочий документ: фича закрыта — строка обновляется в том же коммите.

Требования — [spec-v2.md](spec-v2.md). Этапы P0–P3 — [раздел 8](spec-v2.md#8-этапы).

## 1. Процесс на каждую фичу

Одна фича — одна ветка — один merge. Ветка не вливается, пока не пройдены все
шаги:

```
ветка от master
   ▼
реализация (агент)          код + тесты + документация в одном коммите
   ▼
ruff · ruff format · mypy · pytest      всё зелёное, иначе шаг не пройден
   ▼
критическое ревью (Sonnet)  ищет дефекты, а не подтверждает работу
   ▼
правки по ревью
   ▼
верификация модуля (Fable)  проверяет, что фича реально работает целиком
   ▼
merge в master → CI: quality → docs-gate → deploy
```

Что означает каждый шаг:

- **Реализация.** Слои и правила — [CLAUDE.md](../CLAUDE.md). Добавление
  источника не меняет ни одной существующей строки; если пришлось править
  планировщик или воронку — абстракция протекла, и это повод переделать.
- **Тесты.** Адаптеры — на зафиксированных фикстурах ответов, не живой сетью.
  LLM в тестах замокан. Тест, который ходит в модель, не тест.
- **Критическое ревью.** Отдельный агент на модели Sonnet, задача — найти
  дефекты. Ревью, которое ничего не нашло, требует объяснения, почему.
- **Верификация.** Отдельный агент проверяет собранный модуль: сценарий целиком
  на живых данных, а не отдельные функции. Модель — **отличная от той, что
  делала ревью**: одна и та же модель склонна не заметить то же самое дважды.
  Ревью ведёт Sonnet, верификацию — Opus (решение владельца от 31.08.2026,
  Fable из конвейера исключён).
- **Документация.** Пункт 3 Definition of Done: меняется поведение — меняется
  описание, в том же коммите. Разъехавшаяся документация хуже отсутствующей.
- **Безопасность.** Секретов в диффе нет; каждый внешний ввод (текст клиента,
  ответ источника, ответ модели) считается недоверенным.

## 2. Состояние на 2026-08-31

### Готово

| Модуль | Что умеет |
|---|---|
| `runtime/` | каркас процесса: логи, сигналы, ожидание конфигурации |
| `config.py` | настройки из окружения |
| `broker/` | клиент AIbroker: submit + поллинг, терминальный cap |
| `domain/passport.py` | паспорт запроса, выбор уточняющих вопросов |
| `search/` | intake (LLM + правила), планировщик, план, бюджет, фолбэк, исполнение |
| `sources/chotot.py` | адаптер Chotot, параметры сняты живыми запросами |
| `sources/telegram_*.py` | адаптер telegram_groups: `messages.search` по чатам реестра, бюджет 10 чатов и 40 с на источник, последовательный обход, FloodWait с растущей паузой, форум-топики, альбомы, распознавание мёртвой сессии. Проверен живым аккаунтом; реестр чатов подключён к боевому пути (`new_directory()` → `ChatRepository`) |
| `bot/` | первый диалог, карточки выдачи |
| `verifier/liveness.py` | возраст лота, пометка `stale` |
| `pipeline/gate.py` | мультиязычный regex-гейт |
| `db/` | engine и сессии, ORM-зеркало схемы, репозитории chats · raw_messages · listings · users · passports, очередь `jobs` на `FOR UPDATE SKIP LOCKED` |
| CI | `quality.yml` (с Postgres-контейнером под тесты `db/`), `deploy.yml` |

185 тестов зелёные; ещё 17 требуют живой базы и идут в CI — локально без
Docker они пропускаются по отсутствию `TEST_DATABASE_URL`, а проверки файла
сессии (права 0600, симлинки, каталог без права обхода) — по отсутствию POSIX
вне Linux.

### Не сделано

| Пробел | Почему это блокирует |
|---|---|
| SQL-фильтр по городу в `ChatRepository` | сейчас город отрезается в Python после выборки с пятикратным запасом; при десятках чатов это дешевле, но с ростом реестра запас перестанет спасать |
| таблицы `sources`, `listing_verifications`, `usage_counters`, `payments`, `price_history`, `users.tier` | описаны в spec-v2, в `001_init.sql` отсутствуют |
| `collector/`, `worker/`, `notifier/` | только точки входа, тела нет |
| `pipeline`: prefilter, extract, dedup | воронка обрывается на гейте |
| `verifier`: grounding, passport_match | карточки не проверяются на соответствие исходнику |
| `matching/` | пусто: нет запроса по паспорту и ранжирования |
| развёртывание на `195.201.31.49` | каталога `/var/www/sniffer` нет, контейнеров нет |
| таблица `chats` пуста | кандидаты сняты и разложены по волнам ([chats-nha-trang.md](chats-nha-trang.md)), но юзербот пока состоит в двух непрофильных группах: пока `feat/telegram-discover` не вступил и не завёл записи, реестр читается вхолостую |

## 3. Очередь фич

Волна — это набор веток, которые можно вести параллельно: они не пересекаются
по файлам. Внутри волны порядок не важен, между волнами — важен.

### Волна 1 — фундамент

| Ветка | Содержание | Зависит от |
|---|---|---|
| ~~`feat/db-layer`~~ | сделано: async engine, модели, репозитории, единственное место с SQL | — |
| `feat/schema-v2` | недостающие таблицы + `users.tier`, миграция на живой базе | `db-layer` |
| ~~`feat/telegram-groups-adapter`~~ | **сделано:** `messages.search` по известным чатам, FloodWait с экспоненциальной паузой, детали Telethon — [spec-v2, 4.4](spec-v2.md#44-telegram-группы--что-стоило-дорого-узнать) | — |

`db-layer` блокировал почти всё, поэтому шёл первым и один. Он закрыт —
устройство слоя описано в [architecture.md, раздел 5.1](architecture.md#51-слой-доступа--db).
Репозиториев ровно столько, сколько нужно сейчас: подписки, уведомления и
outbox своих классов ещё не получили, их заводят волны 3 и 4.

Адаптер `telegram_groups` слоя `db` не ждал: реестр чатов объявлен протоколом
`ChatDirectory` и подставляется снаружи. Дописывать в `db` ничего не пришлось —
`ChatRepository.list_active` уже есть; сборка живёт в `new_directory()` и стоит
значением по умолчанию, а обёртка берёт на себя сессию, город и запас выборки —
[spec-v2, 4.4](spec-v2.md#44-telegram-группы--что-стоило-дорого-узнать).

Урок оттуда стоит держать при себе на каждой следующей ветке: **строка сборки,
которая живёт только в документации и тестах, боевого пути не касается.** Здесь
она отсутствовала неделю: адаптер был написан, проверен живым аккаунтом и
покрыт тестами, а `run_plan` создавал источник через `get_source(name)` без
аргументов и получал заглушку — главный источник продукта возвращал ноль
находок при исправном коде. Ловит это только тест, который идёт боевым путём
целиком (`tests/test_source_wiring.py`); тест на обёртку в отрыве от сборки
проходил и до проводки.

Отдельной задачей на потом: **SQL-фильтр по городу в `ChatRepository`**. Сейчас
город отрезается в Python после выборки с пятикратным запасом — при десятках
чатов это дешевле лишнего условия в запросе, но с ростом реестра запас
перестанет спасать, и фильтр придётся опустить в SQL.

### Волна 2 — воронка и проверка

| Ветка | Содержание | Зависит от |
|---|---|---|
| `feat/pipeline-prefilter` | ступень 2 воронки, батчи по 20 | db-layer |
| `feat/pipeline-extract` | ступень 3, strict `json_schema`, батчи по 10 | db-layer |
| `feat/dedup` | `(source, external_id)` + хэш нормализованного текста | db-layer |
| `feat/verifier-grounding` | spec-v2 3.1: поле без подтверждения обнуляется | extract |
| `feat/verifier-passport-match` | spec-v2 3.2: deal-breaker выбрасывает карточку | schema-v2 |

### Волна 3 — ингест и подбор

| Ветка | Содержание | Зависит от |
|---|---|---|
| `feat/collector-ingest` | live-подписка, догон по `last_msg_id`, `StringSession` в БД | telegram-groups |
| `feat/worker-queue` | очередь `jobs` через `FOR UPDATE SKIP LOCKED` | волна 2 |
| `feat/matching` | паспорт → SQL, формула ранжирования | schema-v2 |

### Волна 4 — продукт

| Ветка | Содержание |
|---|---|
| `feat/subscriptions` | паспорт в статусе `active`, матчинг новых карточек |
| `feat/notifier-outbox` | доставка, троттлинг, дайджест, дедуп уведомлений |
| `feat/quotas-antispam` | `tier`, `usage_counters`, rate-limit, капча при первом запуске |
| `feat/liveness-full` | остальные сигналы spec-v2 3.3, `price_history` |

### Волна 4.5 — требования владельца от 31.08.2026

| Ветка | Содержание | Оговорка |
|---|---|---|
| ~~`feat/forum-topics`~~ | **сделано в адаптере:** тема берётся из `reply_to`, ссылка становится трёхсегментной | осталось учесть темы в ингесте, когда он появится |
| ~~`feat/album-dedup`~~ | **сделано в адаптере:** альбом схлопывается по `(чат, grouped_id)` на весь план | воронке `grouped_id` отдан в `raw` — собрать фото в одну карточку она сможет |
| `feat/session-watchdog` | **распознавание сделано в адаптере** (`UnauthorizedError` / `AuthKeyError` на первой же ошибке → `degraded` и остановка обхода); осталось уведомление владельцу в Telegram | требует, чтобы владелец один раз нажал «Старт» у бота |
| `feat/dashboard` | веб-интерфейс владельца — [dashboard.md](dashboard.md) | отменяет свойство «ни один процесс не слушает порт»; нужны nginx и TLS |

Вступление в чаты — **только автоматическое**, через `feat/telegram-discover`
с лимитами из [CLAUDE.md](../CLAUDE.md). Ручное вступление владельцем отменено
его решением: пусть работает то, что поедет в прод, а не обходной путь.

### Волна 5 — расширение

| Ветка | Содержание | Оговорка |
|---|---|---|
| `feat/telegram-discover` | `resolve_username` по словарю кандидатов | вступление автоматическое, лимиты в [CLAUDE.md](../CLAUDE.md) |
| `feat/photo-fingerprint` | перцептивный хеш фото для дедупа кросспостов | **фото не храним**, см. ниже |
| `feat/web-source` | поиск и чтение страниц вне Telegram и Chotot | отложено, см. ниже |

### Фото: хеш вместо хранения — R2 из плана убран (31.08.2026)

Изначально планировалось складывать фотографии объявлений в Cloudflare R2.
Решение отменено: оно противоречило собственному правилу «не перепечатываем
объявления, а даём ссылку на оригинал» — по ссылке фото и так видны, а хранить
чужие снимки у себя это и есть перепечатка.

Настоящая польза от фото другая: один байк выкладывают в пяти группах разные
люди, текст каждый переписывает, а **фотографии одинаковые** — они от
продавца. Дедуп по тексту такое не ловит, перцептивный хеш ловит.

Для этого хранить фото не нужно: скачали, посчитали отпечаток в 16 байт,
картинку выбросили. Хранится отпечаток. R2, его ключи и отдельная задача под
медиа отменяются; единственная оставшаяся причина хотеть R2 — миниатюры в
веб-интерфейсе, и она отдельного сервиса не стоит.

### Веб-источник: отложен, и почему

Нужны две разные вещи: поисковик (даёт ссылки) и читалка страниц (одна ссылка
бесполезна). SearXNG — свой поисковик, Firecrawl — читалка, и она тянет
headless-браузер: гигабайт с лишним и пики на каждой странице. На машину, где
живут Вера, Степан и AIbroker, это ставить нельзя
([deploy.md, раздел 2](deploy.md#2-требования-к-машине)).

Решение — **отложить**. Полевые заметки ([spec-v2, раздел 7](spec-v2.md#7-полевые-уроки--откуда-взялись-требования))
говорят, что вьетнамцы байки в Telegram не продают вообще, их рынок — Chotot,
и он уже подключён; русскоязычный рынок — телеграм-группы, они на подходе.
Веб закрывает не «весь остальной рынок», а его остатки.

Когда понадобится, порядок такой:
1. **Проверить, нужен ли браузер вообще.** Firecrawl необходим только для
   страниц, собираемых джаваскриптом. Вьетнамские доски в основном отдают
   готовый HTML — тогда хватает обычного запроса и небольшой библиотеки
   разбора, десятки мегабайт вместо гигабайта. Проверяется одним
   экспериментом: скачать три целевые страницы и посмотреть, есть ли в них
   текст объявления.
2. Если браузер всё же нужен — **вторая машина** (~4 €/мес). Проект перестаёт
   быть бесплатным, это осознанная плата.
3. Платный поисковый API — дороже второй машины без выигрыша (Brave убрал
   бесплатный тариф в феврале 2026).

## 4. Развёртывание — отдельная задача, не фича

На `195.201.31.49` проекта нет: нужен каталог, deploy-ключи, `.env`, первый
`docker compose up`, авторизация юзербота подкомандой `auth`. Процедура —
[deploy.md, раздел 3](deploy.md#3-первичная-установка--один-раз-руками).

Секреты GitHub (`SSH_HOST`) указывают на удалённую машину — до их обновления
конвейер деплоя не работает.

## 5. Порядок закрытия этапов

P0 закрывается, когда `telegram_groups` начинает приносить выдачу и она
полезна на реальном запросе про байк. До этого момента расширять источники и
строить подписки бессмысленно: [spec-v2, раздел 8](spec-v2.md#8-этапы) прямо
называет эту точку нетехнической.
