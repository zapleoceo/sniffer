"""Тесты пакетом, а не набором файлов.

Нужно затем, чтобы общие фикстуры и типы из `conftest.py` импортировались явно
(`from tests.conftest import DashboardEnv`), а не через неявное добавление
каталога в `sys.path`: неявный путь ломается ровно тогда, когда его меняют.
"""
